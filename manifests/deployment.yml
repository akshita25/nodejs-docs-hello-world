apiVersion : apps/v1
kind: Deployment
metadata:
  name: "hello-world"
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: "hello-world"
    spec:
      containers:
        - name: "hello-world"
          image: "akshita5/simplewhale:latest"
          ports:
          - containerPort: 1337